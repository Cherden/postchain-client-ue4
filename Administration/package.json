{
  "name": "chromia-unity-admin",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "init-blockchain": "node init-blockchain",
    "docker-node": "docker-compose up -d && npm run init-blockchain",
    "docker-node:update": "docker exec -i blockchain update",
    "docker-node:reset": "docker-compose down && docker-compose up -d && npm run init-blockchain",
    "docker-node:stop": "docker-compose down",
    "rell:tests": "node rell-tests.js",
    "start": "npm run docker-node:reset",
    "stop": "npm run docker-node:stop",
    "test": "npm run rell:tests"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@snieking/fs-client": "^0.0.6",
    "bip32": "^2.0.6",
    "bip39": "^3.0.4",
    "dotenv": "^8.2.0",
    "fs-extra": "^9.0.0",
    "ft3-lib": "^3.4.2",
    "postchain-client": "^0.9.2",
    "requestretry": "^5.0.0",
    "tree-kill": "^1.2.2"
  }
}
